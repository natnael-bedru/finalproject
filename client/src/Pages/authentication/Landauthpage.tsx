import React from "react";
import { Outlet } from "react-router-dom";

type Props = {};

const Landauthpage = (props: Props) => {
  return (
    <>
      <Outlet />
    </>
  );
};

export default Landauthpage;
